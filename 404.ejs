<%- include('partials/header', { page: '' }) %>

<section class="form-section">
  <div class="form-card glass">
    <h2>Página não encontrada</h2>
    <div class="info-box">
      O link que tentaste abrir não existe ou foi movido.
    </div>

    <div class="hero-buttons" style="justify-content: flex-start; margin-top: 18px;">
      <a href="/dashboard" class="btn btn-primary">Voltar ao Dashboard</a>
      <a href="/login" class="btn btn-secondary">Ir para Login</a>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
